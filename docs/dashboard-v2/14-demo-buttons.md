# Demo Buttons

The demo buttons operate on the selected bed.

`Pressure Risk` simulates high pressure in two zones.

`IV Ending Soon` simulates a low remaining IV level.

`Reset` restores the initial ward state.
