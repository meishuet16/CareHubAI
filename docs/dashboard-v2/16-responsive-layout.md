# Responsive Layout

Dashboard v2 is designed to work on desktop and smaller screens.

Desktop uses a sidebar and multi-column command layout. Narrow screens collapse into a single-column layout while preserving the same information order.
