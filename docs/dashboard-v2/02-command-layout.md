# Command Layout

The v2 layout uses a sidebar plus main workspace structure.

The sidebar establishes the prototype context. The workspace contains summary metrics, a priority worklist, selected-bed details, ward cards, and an event log.

This layout supports the core pitch: nurses should see which bed needs attention first.
