# IV Status

The IV status panel shows remaining infusion time or paused flow.

In the prototype, IV values are simulated. Future hardware can map load-cell readings or optical drip readings into the same dashboard data model.
